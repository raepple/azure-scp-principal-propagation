---
applications:

- name: service
  memory: 1024M
  timeout: 300
  random-route: true
  path: application/target/service-application.war
  buildpacks:
    - sap_java_buildpack
  env:
    TARGET_RUNTIME: tomee7
    SET_LOGGING_LEVEL: '{ROOT: INFO, com.sap.cloud.sdk: INFO}'
    JBP_CONFIG_SAPJVM_MEMORY_SIZES: 'metaspace:128m..'
  routes:
    - route: service-7e1d613etrial.cfapps.eu10.hana.ondemand.com
  services:
    - service-xsuaa
    - service-destination
    - service-connectivity